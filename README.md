# R3

为 kumokumo 构建的 R3-AOT-Compatible 分支
1. 去除了 `ReactiveProperty<T>` 的自动序列化反序列化
2. 去除了 `EnableValidation<TClass>` 泛型运行时构建

---

R3-AOT-Compatible branch for my own use
1. Removed automatic serialization and deserialization support for ReactiveProperty<T>.
2. Removed runtime generic construction used by EnableValidation<TClass>.
